{
  "name": "discord-creator-asset-studio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node --experimental-strip-types scripts/verify-rules.ts && astro build",
    "preview": "astro preview",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "verify:live": "node scripts/verify-live.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name=discord-creator-asset-studio --force --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
